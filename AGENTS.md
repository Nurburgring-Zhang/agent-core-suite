# AGENTS.md —— Agent Core Suite v1.1.0

> 本文件是**跨终端通用入口**（AGENTS.md 约定被主流 agent 终端自动读取）。
> 无论你是哪个终端上的 agent，进入本仓库后，以下内容为**强制工作标准**。

## 定位：增强层，不是替代层

本套件**叠加在你原有能力之上，只做加法**：

1. **不禁用、不接管、不改写**你终端原生的工具、检索能力、任务清单、记忆系统、权限与审批机制。
2. **原生优先**：你已有等价能力（原生任务清单/计划、原生检索子代理、原生记忆）就**用原生的**；本套件的 `templates/` 仅在原生缺位时兜底，**禁止另起一套并行体系**。
3. 本套件只补三样原生普遍缺失的东西：① 分级触发的执行 SOP（何时该重、何时该轻）② 可机检的准出退出码（把「我觉得做完了」变成 0/1）③ 成本闸门的量化阈值（token 与时间硬上限）。
4. **冲突裁决顺序**：主人显式指令 > 终端原生安全/权限/审批约束 > 本套件硬门禁 > 本套件软约束。**本套件永不覆盖前两者。**
5. 下文「强制」的对象是**标准与证据**，不是具体工具路径：用任何等价手段达成都行，只要证据可机检。

## 立即执行

1. 非闲聊任务 → 先读 `skills/universal-task-code/SKILL.md`，按其五步走。
2. 需要更细规则时**按需**加载（禁止一次全装）：

| 场景 | 加载 |
| --- | --- |
| 定停止条件 / 收敛 / 重试 / 判空转 | `skills/loop-engineering/SKILL.md` |
| 拆并行子任务 / 节点契约 / 派子 agent | `skills/graph-engineering/SKILL.md` |
| T3 方案竞标 / 打分排序 / 方向偏离检测 | `skills/self-verify-scaling/SKILL.md` |
| 上下文吃紧 / 压缩总结 / 续跑 | `skills/token-thrift/SKILL.md` |

3. 有硬门禁脚本就必须跑，**不许用「我已检查」替代脚本退出码**：

```bash
python -X utf8 scripts/run_gates.py --state .acs/task-state.json --root . --tier T2
```

`0=PASS`｜`1=BLOCK`｜`2=USAGE_ERROR`。后两者一律视为**未验证 = 未完成**。

## 不可协商的五条

1. **三重零容忍**：零虚假、零模拟实现、零降级。必须降级则显式标注 `[DEGRADED]` 并如实上报。
2. **单步一产出**：一步只交一个可验证产出，预算 ≤30min，完成标准必须是命令/文件/断言，不能是「功能正常」。
3. **禁止回灌**：跨步只传 ≤400 字总结 + `.acs/task-state.json` 指针；单步引用上文 ≤20000 字节、读文件 ≤5 个。
4. **two-strike 停止**：连续 2 步没有新证据（文件变更 / 新通过断言 / 新外部事实）→ 立即停止并升级，禁止同法重试。重试必须登记 `retries` 并写清 `retry_reason` 与 `delta_from_last`（这次与上次差在哪），超上限即换路（v1.1 有界重试，机检）。
5. **思考占比 ≤0.40**：超线就去写最小可执行验证，不要继续空想。

## 目录地图

```
skills/      五个技能：1 个瘦入口 + 4 个按需子技能（软约束）
scripts/     六个门禁脚本，零第三方依赖，纯 stdlib（硬约束；Node 镜像同覆盖）
templates/   task-state / verify-record 的 schema + 正样本 + 交接总结模板
rules/       工作区常驻规则（Qoder 等支持 rules 的终端）
tests/       377 项双向验证（正样本 PASS / 负样本 BLOCK + 套件卫生机检）
DESIGN.md    设计基线与证据表 E1-E27，每条硬约束可追溯
```

## 开工前先建状态文件

```bash
mkdir -p .acs
cp templates/task-state.example.json .acs/task-state.json   # 然后按真实任务改写
```

`task-state.json` 是跨步唯一状态载体。**改它，而不是把历史再贴一遍。**

## 为什么这么严（一句话依据）

某 27B 开源模型在 Agent Harness 上完成 9 个任务烧掉 **13,995,350 token / 197 请求 / 6h17m**，
输入 token 占 **98%**，单题最高 **11.5M token**；它拆了 **75 个 step** 仍大量空转，
「其它运行时间」占单题总时长 **59%**。根因是单步过重、完成标准不清、上下文反复回灌。
本套件的每一条硬阈值都在直接对症这些数字。
